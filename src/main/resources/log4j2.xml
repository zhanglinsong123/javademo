<?xml version="1.0" encoding="UTF-8"?>
<!--日志级别以及优先级排序: OFF > FATAL > ERROR > WARN > INFO > DEBUG > TRACE > ALL -->
<!--Configuration后面的status，这个用于设置log4j2自身内部的信息输出，可以不设置，当设置成trace时，你会看到log4j2内部各种详细输出 -->
<!--monitorInterval：Log4j能够自动检测修改配置 文件和重新配置本身，设置间隔秒数 -->
<configuration status="debug"  monitorInterval="5">
    <!--变量配置-->
    <Properties>
        <Property name="serviceName">java-demo</Property>
        <Property name="filePath">./logs</Property>
    </Properties>


    <!--先定义所有的appender -->
    <appenders>
        <!--这个输出控制台的配置 -->
        <console name="Console" target="SYSTEM_OUT" follow="false">
            <!--输出日志的格式 -->
            <!--            <PatternLayout charset="utf-8" pattern="%highlight{%date{yyyy-MM-dd HH:mm:ss.SSS} [%X{X-B3-TraceId},%X{X-B3-SpanId},%X{X-B3-ParentSpanId},%X{X-Span-Export}] %level [%thread][%file:%line] - %msg%n}" />-->
            <PatternLayout charset="utf-8"
                           disableAnsi="false"
                           pattern="[%highlight{%t}] %d [distId:%X{id},traceId:%X{X-B3-TraceId},spanId:%X{X-B3-SpanId},parentSpanId:%X{X-B3-ParentSpanId},%X{X-Span-Export}] %highlight{%-5level}{FATAL=red blink,ERROR=red, WARN=yellow bold, INFO=green, DEBUG=green bold}  %n      %logger{36}(%F:%L) - %highlight{%msg}%n%throwable"/>
        </console>
        <RollingFile name="error-log" fileName="${filePath}/${serviceName}-error.log"
                     filePattern="${filePath}/$${date:yyyy-MM}/${serviceName}-error-%d{yyyy-MM-dd}-%i.log">
            <!--控制台只输出level及以上级别的信息（onMatch），其他的直接拒绝（onMismatch） -->
            <Filters>
                <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>

            <CustomPatternLayout>
                <!--msg必须放最后，详见CustomPatternLayout -->
                <pattern>
                    [%highlight{%t}] %d
                    [traceId:%X{X-B3-TraceId},spanId:%X{X-B3-SpanId},parentSpanId:%X{X-B3-ParentSpanId},%X{X-Span-Export},%X{id}]
                    %highlight{%-5level}{FATAL=red blink,ERROR=red, WARN=yellow bold, INFO=green, DEBUG=green bold} %n
                    %logger{36}(%F:%L) - %highlight{%msg}%n%throwable
                </pattern>
            </CustomPatternLayout>

            <!--            <PatternLayout charset="utf-8" pattern="%d{yyyy-MM-dd HH:mm:ss:SSS} [${serviceName},%X{X-B3-TraceId},%X{X-B3-SpanId},%X{X-B3-ParentSpanId},%X{X-Span-Export}] %p - %l - %m%n" />-->
            <Policies>
                <!-- 基于时间的触发策略。该策略主要是完成周期性的log文件封存工作 interval，integer型，指定两次封存动作之间的时间间隔;modulate，boolean型，说明是否对封存时间进行调制 -->
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="100MB"/>
            </Policies>
            <DefaultRolloverStrategy>
                <Delete basePath="${filePath}" maxDepth="2">
                    <!--如果文件名匹配指定的 glob 模式，这个条件将会满足。在这里，文件名需要匹配 */*-info-*.log 这个模式，表示删除包含 -info- 的日志文件。-->
                    <IfFileName glob="*/*-error-*.log"/>
                    <!--这段 XML 配置指定了在文件最后修改时间满足指定条件的情况下执行的操作。
                    如果文件最后修改时间距离当前时间不超过 3 天，并且累计文件大小超过 5GB 或者累计文件数量超过 50 个，则执行删除操作。-->
                    <IfLastModified age="3d">
                        <IfAny>
                            <!--文件最大阀值5G，文件个数最大50-->
                            <IfAccumulatedFileSize exceeds="5GB"/>
                            <IfAccumulatedFileCount exceeds="50"/>
                        </IfAny>
                    </IfLastModified>
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>
        <RollingFile name="info-log" fileName="${filePath}/${serviceName}-info.log"
                     filePattern="${filePath}/$${date:yyyy-MM}/${serviceName}-info-%d{yyyy-MM-dd}-%i.log">
            <!--控制台只输出level及以上级别的信息（onMatch），其他的直接拒绝（onMismatch） -->
            <Filters>
                <ThresholdFilter level="INFO" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>

            <CustomPatternLayout>
                <!--msg必须放最后，详见CustomPatternLayout -->
                <pattern>
                    {"date":"%d{yyyy-MM-dd HH:mm:ss:SSS}","serviceName":"${serviceName}","traceId":"%X{X-B3-TraceId}","spanId":"%X{X-B3-SpanId}","parentSpanId":"%X{X-B3-ParentSpanId}","export":"%X{X-Span-Export}","distId":“%X{id}”,"thread":"%thread","level":"%p","file":"%l","msg":"%m"}
                </pattern>
            </CustomPatternLayout>

            <!--            <PatternLayout charset="utf-8" pattern="%d{yyyy-MM-dd HH:mm:ss:SSS} [${serviceName},%X{X-B3-TraceId},%X{X-B3-SpanId},%X{X-B3-ParentSpanId},%X{X-Span-Export}] %p - %l - %m%n" />-->
            <Policies>
                <!-- 基于时间的触发策略。该策略主要是完成周期性的log文件封存工作 interval，integer型，指定两次封存动作之间的时间间隔;modulate，boolean型，说明是否对封存时间进行调制 -->
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="100MB"/>
            </Policies>
            <DefaultRolloverStrategy>
                <Delete basePath="${filePath}" maxDepth="2">
                    <!--如果文件名匹配指定的 glob 模式，这个条件将会满足。在这里，文件名需要匹配 */*-info-*.log 这个模式，表示删除包含 -info- 的日志文件。-->
                    <IfFileName glob="*/*-info-*.log"/>
                    <!--这段 XML 配置指定了在文件最后修改时间满足指定条件的情况下执行的操作。
                    如果文件最后修改时间距离当前时间不超过 3 天，并且累计文件大小超过 5GB 或者累计文件数量超过 50 个，则执行删除操作。-->
                    <IfLastModified age="3d">
                        <IfAny>
                            <!--文件最大阀值5G，文件个数最大50-->
                            <IfAccumulatedFileSize exceeds="5GB"/>
                            <IfAccumulatedFileCount exceeds="50"/>
                        </IfAny>
                    </IfLastModified>
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>
    </appenders>
    <!--然后定义logger，只有定义了logger并引入的appender，appender才会生效 -->
    <loggers>
        <root level="Info">
            <appender-ref ref="Console"/>
            <appender-ref ref="info-log"/>
<!--            <appender-ref ref="local-log"/>-->
            <appender-ref ref="error-log"/>
        </root>
    </loggers>
</configuration>
